# Validation — oauth-only-0.3.0

**Verifier:** independent (not the author). **Branch:** `feat/oauth-only-0.3.0` (untouched: no checkout, no commit, no `make e2e-up`/docker).
**Date:** 2026-07-22. **Method:** evidence-or-zero — every row below is a command output or a file:line, nothing taken on the author's word.

## Verdict: **PASS with one flagged gap (non-blocking per literal AC scope, but real)**

AC-1..AC-4 are satisfied as literally scoped in spec.md. AC-5 is correctly left undone and documented as pending. The one real finding is that the **e2e test suite** (`tests/e2e/`, not in AC-1's file list) still asserts the removed OTP tool contract — it was not in scope of any AC's grep/file list, but it directly contradicts the spec's "total purge" decision and would fail against the real upstream (the tools genuinely no longer exist). See gap #1.

## AC → evidence

| AC | Evidence | Result |
|----|----------|--------|
| **AC-1** otp grep in 3 skills | `grep -rin "otp" skills/konecty-data skills/konecty-meta skills/konecty-setup` → only 3 hits, all `otpConfig` in `konecty-meta/references/namespace.md:66,131,133`, each annotated "unrelated to MCP auth" / "Konecty UI login" | PASS |
| **AC-1** authTokenId grep | `grep -rin "authtokenid" skills/konecty-data skills/konecty-meta skills/konecty-setup` → 0 hits (exit 1) | PASS |
| **AC-1** session_* tool names in skills/ | `grep -rin "session_login_options\|session_request_otp\|session_verify_otp\|session_logout" skills/` → 0 hits | PASS |
| **AC-1** session_* tool names in tests/ | Same grep against `tests/` → **hits** in `tests/e2e/test_client_smoke.py:20-25,86` (USER_TOOLS set) and `tests/e2e/test_user_mcp.py:65-85` (whole `test_otp_session_flow_with_per_tool_token` test) | **GAP** (see #1) |
| **AC-1** auth.md sample read | `skills/konecty-data/references/auth.md` (28 lines) — OAuth-only, host-driven, no OTP fallback. `skills/konecty-meta/references/auth.md` (42 lines) — OAuth trusted-client only (ADR-0011), errors table has no OTP row. `skills/konecty-setup/references/troubleshooting.md` (38 lines) — "consent screen missing" / "admin option not showing" rows point to backend upgrade or `OAUTH_CLIENTS_JSON`, never OTP | PASS |
| **AC-1** konecty-setup SKILL.md | File is 213 lines total (spec's cited hotspot `174-225` no longer exists at that size — consistent with the purge shrinking the file); "Flow: admin path" section (`SKILL.md:180-198`) is OAuth-trusted-client only | PASS |
| **AC-2** installer grep | `grep -rin "otp\|admin-auth\|KONECTY_TOKEN" installer/src installer/tests` → 1 hit: `installer/tests/test_installer.py:506` `cred_path.write_text("KONECTY_TOKEN=secret\n")` — a generic fixture for the (unrelated) credentials-file-purge-on-uninstall test, not an OTP/admin-auth artifact. Justified. | PASS |
| **AC-2** `make installer-test` | `Ran 197 tests in 0.229s` / `OK` | PASS |
| **AC-2** `make lint` | `py_compile OK` (byte-compiles installer + skills + e2e + tests) | PASS |
| **AC-2** breaking change recorded | `docs/changelog/2026-07-22_release-0.3.0-oauth-only.md:8-15` documents `--admin-auth otp` removal, `KONECTY_TOKEN`/`~/.konecty/.env` flow removal, 197 green / ~30 removed tests (confirmed by `git diff --stat main...HEAD -- installer/tests/` → 408 deletions across 5 files, consistent with the claim) | PASS |
| **AC-3** `files.md` contract | `skills/konecty-data/references/files.md:25-50` — input `document/recordId/fieldName/fileName`, output `uploadUrl/expiresAt/method/maxFileSize`, single-use + 410 on reuse, curl example, chat-only limitation (rule 4) | PASS, matches Konecty's `.specs/features/mcp-file-upload-url/spec.md` AC-1 exactly (same 4 inputs, same 4 outputs) and matches the actual implementation `src/mcp/user/tools/files.ts:19-79` (`toMcpSuccessResult({ uploadUrl, expiresAt, method: 'POST', maxFileSize }, ...)`) |
| **AC-3** `remove.md` + SKILL.md | `skills/konecty-meta/references/remove.md` — dry-run without `confirm` shows blast radius (record count, inbound lookups, related metas), `confirm: true` deletes, `MetaObjects.Trash`, Namespace undeletable, one-object-per-call, children→hooks→primary order. `skills/konecty-meta/SKILL.md` tool table has exactly 13 `meta_*` rows (`grep -c "^| \`meta_" SKILL.md` → 13) | PASS, matches Konecty's `.specs/features/admin-mcp-meta-delete/spec.md` (AC-1/AC-3: dry-run blast radius, `confirm:true`, `MetaObjects.Trash`, namespace refused) and the actual implementation `src/mcp/admin/tools/metaDelete.ts:52-90` |
| **AC-4** version bumps | `pyproject.toml:11` `0.3.0`; `installer/src/konecty_skills/__init__.py:6` `0.3.0`; `.claude-plugin/plugin.json:5` `0.3.0`; `.claude-plugin/marketplace.json:8,17` `0.3.0` | PASS |
| **AC-4** changelog | `docs/changelog/2026-07-22_release-0.3.0-oauth-only.md` exists; `docs/changelog/README.md:7` has the index row for `2026-07-22` | PASS |
| **AC-4** `claude plugin validate .` | `Validating marketplace manifest: .../marketplace.json` → `✔ Validation passed` | PASS |
| **AC-4** `make validate` | **Fails**: `gh skill publish --dry-run` (run from inside each skill dir) errors `name "konecty-data" does not match directory name "."`. Reproduced directly (`cd skills/konecty-data && gh skill publish --dry-run`, and `gh skill publish skills/konecty-data --dry-run` from repo root — same error both times) — this is a `gh skill` CLI **preview-feature bug** comparing the frontmatter `name` against the literal string `"."` instead of the resolved directory basename, not a defect in this repo's SKILL.md files. Pre-existing tool limitation, unrelated to the OAuth-only changes (SKILL.md `name:` fields were not touched by this feature). Also surfaces a `license` field warning (pre-existing, not part of any AC) | **make target fails, but root cause is an unrelated environment/CLI bug, not a spec regression** |
| **AC-5** documented as pending | `.specs/features/oauth-only-0.3.0/tasks.md:20` — T7 unchecked, explicitly: validate `/plugin marketplace add konecty/skills` in a real session, then `make publish`, "registrar resultado no changelog/STATE" — not executed (per instructions, correctly not run here) | PASS (documented, correctly not executed) |
| **Full suite** | `uv run --with pytest python -m pytest tests/ installer/tests/ -q` → `197 passed, 39 skipped in 0.32s`. All 39 skips are e2e tests auto-skipped by `requires_stack` (`tests/e2e/conftest.py:52`, "e2e stack unreachable at http://localhost:3200") — no live stack was started, per instructions | PASS |

## Gaps (ranked)

1. **[HIGH] Stale OTP contract in e2e tests, undetected by AC-1's literal scope.** `tests/e2e/test_client_smoke.py:18-25` (`USER_TOOLS` still lists `session_login_options`, `session_request_otp_email/phone`, `session_verify_otp_email/phone`, `session_logout`) and `tests/e2e/test_user_mcp.py:65-86` (`test_otp_session_flow_with_per_tool_token` calls those exact tools and reads an `authTokenId`) predate the purge commit (`a6740c5`, purge landed after `3154a59` which wrote these tests) and were never updated. Confirmed the tools genuinely no longer exist upstream: `grep -rn "session_login_options\|session_request_otp\|session_verify_otp\|session_logout" src/mcp docs/en/mcp.md` in the Konecty repo → 0 hits. If `make e2e-up` were run, `test_user_mcp_exposes_documented_tools` and `test_otp_session_flow_with_per_tool_token` would fail immediately. This directly contradicts spec.md's "total purge" decision even though AC-1's grep list only names `skills/`. `tasks.md` T5 only scoped the e2e rewrite for `file_upload` (done, verified: `test_file_upload_download_delete` correctly uses the new `uploadUrl`/410 contract) — the OTP e2e rewrite was never tasked. Recommend a follow-up task before closing the release cycle.
2. **[LOW] `make validate` red due to an unrelated `gh skill` CLI preview bug** (see AC-4 row) — not a repo defect, but worth noting in STATE.md so it isn't re-investigated as a regression next time `make check`/`make validate` runs.
3. **[LOW] `tasks.md` checkboxes all unchecked** (`T1`–`T7` all `- [ ]`) despite the corresponding commits existing (`a6740c5`, `3747a62`, `1b43660`). Cosmetic — the spec-driven workflow calls for updating task status as decisions close; doesn't affect functional correctness.
4. **[INFO] No `STATE.md` exists yet for this feature** — the repo's `tlc-spec-driven` convention (mirrored from the Konecty repo's AGENTS.md) expects a running decision log; only `spec.md` and `tasks.md` are present.
